/mnt/mongo.log {
        daily
        rotate 30
        compress
        dateext
 
    missingok
    notifempty
    sharedscripts
    postrotate
        /bin/kill -SIGUSR1 `cat /vol/mongodb-data/mongod.lock 2> /dev/null` 2> /dev/null || true
    endscript
}
