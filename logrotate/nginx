/var/log/nginx/*.log {
  daily
  missingok
  rotate 10
  compress
  delaycompress
  notifempty
  postrotate
    test ! -f /opt/nginx/logs/nginx.pid || kill -USR1 `cat /opt/nginx/logs/nginx.pid`
  endscript
}